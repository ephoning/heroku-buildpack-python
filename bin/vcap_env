#!/usr/bin/python

import ast
import os
import sys


def get_application_version():
  return ast.literal_eval(os.environ["VCAP_APPLICATION"])["application_version"]

expr = "os.environ"

if len(sys.argv) > 1:
  expr = sys.argv[1] + "()"

print eval(expr)
